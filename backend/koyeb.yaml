name: bot-inteligente
build:
  commands:
    - corepack enable
    - corepack prepare pnpm@10.12.1 --activate
    - pnpm install --no-frozen-lockfile
    - pnpm build

run:
  cmd: pnpm start
  env:
    - name: NODE_ENV
      value: production
  ports:
    - 3000
  working_dir: backend
